name: Supabase Migration CI

# Concurrency: separate groups per event + ref so PR validation won't cancel push/dispatch runs
concurrency:
  group: supabase-migration-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'pull_request' }}

on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
  pull_request:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
  workflow_dispatch:
    inputs:
      forceReapplyLatest:
        description: "Force revert and re-apply the latest local migration on remote (use if remote was stamped)"
        required: false
        default: "false"

jobs:
  # Safe validation job for PRs that doesn't touch secrets or database
  validate-migrations:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate migration files
        run: |
          echo "🔍 Validating migration files..."

          # Check if migration files exist and have valid SQL syntax
          migration_files=$(find supabase/migrations -name "*.sql" -type f 2>/dev/null || true)

          if [ -z "$migration_files" ]; then
            echo "❌ No migration files found"
            exit 1
          fi

          echo "Found migration files:"
          echo "$migration_files"

          # Basic SQL syntax validation
          for file in $migration_files; do
            echo "Validating $file..."
            if ! grep -q ";" "$file"; then
              echo "⚠️  Warning: $file may not contain valid SQL statements"
            fi
            echo "✅ $file appears valid"
          done

          echo "✅ Migration validation completed successfully!"

  # Migration job that only runs on push to main or manual dispatch
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Verify environment variables
        run: |
          echo "Checking required environment variables..."
          if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then
            echo "❌ SUPABASE_ACCESS_TOKEN is not set"
            exit 1
          fi
          if [ -z "$SUPABASE_DB_PASSWORD" ]; then
            echo "❌ SUPABASE_DB_PASSWORD is not set"
            exit 1
          fi
          if [ -z "$SUPABASE_PROJECT_ID" ]; then
            echo "❌ SUPABASE_PROJECT_ID is not set"
            exit 1
          fi
          echo "✅ All required environment variables are set"

      - name: Link to Supabase project
        timeout-minutes: 10
        run: |
          echo "🔗 Linking to Supabase project: $SUPABASE_PROJECT_ID"
          supabase link --project-ref "$SUPABASE_PROJECT_ID" \
            --password "$SUPABASE_DB_PASSWORD" --debug

      - name: Check migration status
        timeout-minutes: 10
        run: |
          echo "📋 Checking current migration status..."
          supabase migration list --debug

      - name: Install PostgreSQL client (psql)
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Show remote migration records
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          echo "🧾 supabase_migrations.schema_migrations content:"
          psql "postgresql://postgres.${SUPABASE_PROJECT_ID}:${SUPABASE_DB_PASSWORD}@aws-1-eu-central-1.pooler.supabase.com:6543/postgres" -c \
            "select version, name, coalesce(array_length(statements,1),0) as stmt_count from supabase_migrations.schema_migrations order by version;"

      - name: Optionally force revert latest migration on remote
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.forceReapplyLatest == 'true' }}
        run: |
          set -euo pipefail
          latest_local=$(ls -1 supabase/migrations/*.sql | sort | tail -n1 | xargs -n1 basename | cut -d '_' -f1)
          echo "🔁 Forcing revert of remote version: $latest_local"
          supabase migration repair --status reverted "$latest_local" --debug
          echo "📋 Status after revert:"
          supabase migration list --debug

      - name: Repair migration conflicts (remote-only)
        timeout-minutes: 10
        run: |
          echo "🔧 Checking for migration conflicts..."
          
          # Get the list of remote migrations that aren't local
          remote_only_migrations=$(supabase migration list --debug 2>/dev/null | grep -E '^\s*\|\s*20[0-9]{12}\s*\|' | grep -v 'Local' || true)
          
          if [ -n "$remote_only_migrations" ]; then
            echo "Found remote-only migrations, attempting repair..."
            
            # Extract migration versions
            remote_versions=$(echo "$remote_only_migrations" | awk '{print $2}' | grep '^20[0-9]{12}$' || true)
            
            for version in $remote_versions; do
              echo "Marking migration $version as reverted..."
              supabase migration repair --status reverted "$version" --debug || {
                echo "⚠️  Failed to repair migration $version, continuing..."
              }
            done
          else
            echo "✅ No remote-only migration conflicts detected"
          fi
          
          # Show updated status
          echo "📋 Migration status after repair:"
          supabase migration list --debug

      - name: Run migrations
        timeout-minutes: 30
        run: |
          echo "🚀 Running migrations..."
          # For very large DDL, consider a direct DB URL (uncomment next two lines):
          # export SUPABASE_DB_URL="postgresql://postgres:${SUPABASE_DB_PASSWORD}@db.${SUPABASE_PROJECT_ID}.supabase.co:5432/postgres"
          # supabase db push --debug --db-url "$SUPABASE_DB_URL"
          supabase db push --debug

      - name: Verify migration completion
        timeout-minutes: 10
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          echo "✅ Verifying migration completion..."
          supabase migration list --debug
          echo "🧾 supabase_migrations.schema_migrations content (post-push):"
          psql "postgresql://postgres.${SUPABASE_PROJECT_ID}:${SUPABASE_DB_PASSWORD}@aws-1-eu-central-1.pooler.supabase.com:6543/postgres" -c \
            "select version, name, coalesce(array_length(statements,1),0) as stmt_count from supabase_migrations.schema_migrations order by version;"

      - name: List user tables (non-system schemas)
        timeout-minutes: 5
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          echo "📚 Listing tables in non-system schemas:"
          psql "postgresql://postgres.${SUPABASE_PROJECT_ID}:${SUPABASE_DB_PASSWORD}@aws-1-eu-central-1.pooler.supabase.com:6543/postgres" -Atc \
            "select table_schema, table_name from information_schema.tables where table_schema not in ('pg_catalog','information_schema') order by 1,2 limit 200;"
          echo "Migration workflow completed successfully!"
